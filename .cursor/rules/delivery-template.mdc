---
description: 子步交付强制模板 + 两轮 commit（讲解轮 / 提交轮）
alwaysApply: true
---

# 子步交付模板（缺一 = 未完成）

完成任意 **Mx.y** 后，**第一轮回复必须按顺序包含全部 4 节**。缺任一节不得进入 commit 阶段。

## 固定顺序

### §1 代码讲解（必填，不可省略）

- 按**文件/模块**分节，简体中文
- **每个改动文件**至少 **1 处** `startLine:endLine:path` 行号引用
- 每文件写清：职责 1 句 + 关键函数/字段 + 谁调用谁

### §2 数据流

- mermaid 序列图/流程图，或 numbered 分步列表

### §3 验收

- 具体命令（PowerShell 可跑）+ 期望输出/现象

### §4 场景题

- 2～4 道面经场景题 + 完整参考答案
- 已写入 `docs/qa-mx.md` 则注明「见 qa-mx §…」，并摘要 1 题

---

## 两轮 commit（1+3 约定）

| 轮次 | 用户说什么 | Agent 做什么 |
|------|------------|--------------|
| **第一轮** | 「继续 Mx.y」 | 写代码 + **§1～§4 完整交付**；**不给** commit message |
| **第二轮** | 「ok commit」 / 「提交 Mx.y」 | 只：`git add` → `commit` → `push`（按 PLAN 子步约定） |

**禁止**：第一轮末尾附带 commit 草案（挤占 §1 讲解空间）。  
**禁止**：用户未说 commit 就执行 `git commit`。

---

## 自检（发出前默念）

- [ ] 每个改动文件都有行号引用？
- [ ] 数据流 + 验收 + 场景题都在？
- [ ] 这是第一轮还是第二轮？第一轮无 commit。
